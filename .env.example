# Supabase — copy to .env and fill in your project values
# Get these from: https://supabase.com/dashboard/project/_/settings/api

PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Vercel deploy hook — triggers a rebuild when events are published (SEO/indexing)
# Create in Vercel: Project Settings → Git → Deploy Hooks
VERCEL_DEPLOY_HOOK_URL=https://api.vercel.com/v1/integrations/deploy/your-hook-id

# Optional: service role key for server-side scripts ONLY — never expose to browser
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
