---
import Layout from "@/layouts/Layout.astro";
import { InsightsHubPage } from "@/components/pages/insights-hub-page";
import { fetchAllInsightsForBuild } from "@/lib/insights/articles-build";

const articles = await fetchAllInsightsForBuild();
---

<Layout title="Insights & Writing" description="Essays and field notes on leadership and transformation.">
  <InsightsHubPage client:load initialArticles={articles} />
</Layout>
